[
  {
    "id": "aurora-gradient-hero",
    "title": "Aurora gradient hero",
    "source_url": "https://example.com",
    "media": null,
    "media_type": "image",
    "tags": ["dark-mode", "gradient-mesh", "scroll-animation"],
    "colors": ["#050505", "#7c5cff", "#00e0c6"],
    "notes": "Sample entry — shows the schema. Delete once you have real designs.",
    "recreation_prompt": "Build a full-viewport hero section with a near-black (#050505) background and an animated aurora-style gradient mesh (blurred, softly moving blobs of #7c5cff and #00e0c6 at ~40% opacity, generated with layered radial-gradient divs or an SVG filter, animated via CSS keyframes drifting position over 20s+ ease-in-out loops). Centered headline in a large bold sans-serif, subheading below in a muted gray, and a single pill-shaped CTA button with a subtle glow on hover. On scroll, fade and slightly translate the hero content upward using an IntersectionObserver-driven class toggle (opacity 1→0, translateY 0→-20px), so it feels like the hero dissolves as you scroll past it.",
    "date_added": "2026-07-30"
  },
  {
    "id": "notosan-nature-scene-carousel",
    "title": "Notosan — scroll-driven nature parallax hero",
    "source_url": null,
    "media": "assets/notosan-nature-scene-carousel.mp4",
    "media_type": "video",
    "tags": ["hero-section", "parallax-scroll", "intersection-observer", "fixed-background-crossfade", "scene-transition", "travel", "dark-mode", "storytelling"],
    "colors": ["#0e1339", "#181a3d", "#3a3366", "#7a6995", "#7d76b1", "#946570", "#bb9b8d", "#ffd2cc"],
    "notes": "Travel site hero for a fictional Japanese nature park brand, \"Notosan.\" Originally analyzed as a 10s auto-advancing carousel, but that recreation felt static/choppy in practice. This prompt is the corrected version: a real scroll-driven parallax build (three normal-flow 100vh sections + fixed crossfaded background layers + IntersectionObserver reveals), validated end-to-end and confirmed fluid.",
    "recreation_prompt": "Build a scroll-driven, multi-section hero for a fictional Japanese nature-park travel brand called \"Notosan.\" The page has THREE full-height sections the user scrolls through normally (this is not a fixed/absolute-stacked single-screen carousel) — each section swaps in its own full-viewport background photo and reveals its own heading/copy as it scrolls into view.\n\nGLOBAL LAYOUT:\n- `html { scroll-behavior: smooth; }`. Body is black, white text, `overflow-x: hidden`.\n- Fixed navbar pinned to the top of the whole page (not per-section): wordmark \"Notosan\" on the left; nav links \"Community / About / Support / Contact\" plus a pill-shaped \"Register\" button (1px white border, fills white with black text on hover) on the right. Background is a gradient from `rgba(0,0,0,0.7)` at the top fading to transparent, so it reads over any photo beneath it. `padding: 2rem 4rem`, `z-index: 100`.\n- Typography: pair a serif display face (e.g. Playfair Display) for headings with a geometric sans (e.g. Inter) for everything else — this contrast is part of the look, don't use one font for both.\n\nBACKGROUND LAYERS (the parallax mechanism):\n- Three `position: fixed; top:0; left:0; width:100%; height:120vh; z-index:-1;` divs, one per section, each with its own `background-image` (real photography — a vibrant mountain at sunrise/dusk for section 1, a mystical blue waterfall for section 2, a glowing lush forest for section 3), `background-size: cover`, plus a `::after` dark overlay (`rgba(0,0,0,0.4)`) so white text stays legible over any photo.\n- All three start at `opacity: 0` with `transition: opacity 1s ease-in-out`; only the currently-relevant one carries `.active` (`opacity: 1`) at a time — crossfade, never a hard cut or a slide.\n- On scroll, apply a continuous subtle parallax drift to every background layer (not just the active one, so whichever fades in next is already correctly positioned): `scrollFraction = scrollY / (document.body.scrollHeight - windowHeight)`, `translateY = -(scrollFraction * windowHeight * 0.2)`, applied as `transform: translateY(...)`. The 120vh height (20vh taller than the viewport) is exactly what gives this drift room to move without exposing an edge.\n- Track which section currently owns the background by comparing `scrollY` against each section's `offsetTop - windowHeight/2`, checked from the last section backward (so the furthest-down threshold crossed wins); only touch the DOM (`classList.add/remove('active')`) when the winning section actually changes, not on every scroll tick.\n\nFOREGROUND SECTIONS:\n- Each `<section>` is a normal, real 100vh block (`display:flex; flex-direction:column; justify-content:center; padding: 0 10%;`) — three of them stacked in normal document flow, so the page is genuinely 300vh tall and scrolls like a regular page, not a pinned/absolute single screen.\n- Section 1 (hero): no alignment override, so content sits left via the default flex stretch inside its `max-width: 600px` wrapper. Kicker-style subtitle (\"Journey to new frontiers, Journey to Noto Nature Park\", small, letter-spaced 3px, muted white), a massive serif headline (\"VISITE\", ~7rem, letter-spacing 5px, line-height 1), 2-3 lines of body copy, and a primary CTA: a transparent button with uppercase tracked text plus a circular outlined play-icon badge (50x50px) that fills white with a black icon on hover, and the whole button shifts `translateX(10px)` on hover.\n- Section 2: same structure but `align-items: flex-end; text-align: right;` (content sits on the right). Small numbered tag (\"02 / Experience\"), serif section title (\"Tranquility\", ~5rem), body copy, and a secondary CTA: a plain text link (\"Learn more →\") whose gap between text and arrow grows from 0.8rem to 1.2rem on hover (the arrow visibly slides away, not translateX).\n- Section 3: mirrors section 2 but `align-items: flex-start; text-align: left;` (back on the left). Tag (\"03 / Habitat\"), title (\"Biodiversity\"), copy, same secondary-link CTA style.\n\nSCROLL REVEALS (IntersectionObserver, not scroll-position math):\n- Each section's content wrapper starts as `opacity:0; transform: translateY(50px);` with `transition: all 0.8s cubic-bezier(0.25,0.46,0.45,0.94);`.\n- An `IntersectionObserver` with `{ threshold: 0.3 }` toggles a `.active` class on whichever wrapper is intersecting — add it back off every time the element leaves the viewport too, so the reveal replays if the user scrolls back up and down again, rather than firing once and staying done.\n- Once `.active`, the wrapper's direct children (kicker/heading/copy/CTA) each independently run a `fadeInUp` keyframe (`opacity 0→1`, `translateY(30px)→0`, 0.8s) with staggered `animation-delay` of 0.1s/0.3s/0.5s/0.7s in DOM order, so the text cascades in piece by piece instead of arriving as one block.\n\nRESPONSIVE: below ~768px, hide the nav links (keep logo + Register), and drop the hero title/section titles to roughly 4rem/3rem with reduced section padding.",
    "date_added": "2026-07-30"
  }
]
